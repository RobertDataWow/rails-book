<a href="<%= books_path %>">back</a>
<h1> <%= @book.name %> </h1>
<h3> <%= @book.release.strftime('%B %d, %Y') %> </h3>
<h3> <%= @book.description %> </h3>
<ul>
  <li><%= link_to "Edit", edit_book_path(@book) %></li>
  <li><%= button_to "Delete", book_path(@book), method: :delete%></li>
</ul>
<%= form_for([@book, @book.reviews.build]) do |f| %>
  <%= f.label :comment, "Comment" %><br>
  <%= f.text_area :comment, rows: 4%><br>
  <br>
  <%= f.label :star, "Star Rating" %><br>
  <%= f.select :star, options_for_select([1, 2, 3, 4, 5]), {}%><br>
  <br>
  <%= f.submit "Submit Review"%>
<% end %>
