<!DOCTYPE html>
<html>
  <head>
    <title>Rankings</title>
    <style>
      body {
        font-family: Arial, sans-serif;
      }

      table {
        border-collapse: collapse;
        width: 100%;
        margin-top: 20px;
      }

      th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }

      th {
        background-color: #f2f2f2;
      }

      tr:nth-child(even) {
        background-color: #f2f2f2;
      }
    </style>
  </head>
  <body>
    <%= button_to "Sign out", destroy_user_session_path(destroy_user_session_path), method: :delete %>
    <h1>Ranking</h1>
    <% @ranks.each do |rank| %>
      <h4><%= rank.date.strftime('%Y-%m-%d') %></h4>
      <table>
        <thead>
          <tr>
            <th>Rank</th>
            <th>Book Title</th>
            <th>Views</th>
          </tr>
        </thead>
        <tbody>
          <% rank.book_ranks.each do |book_rank| %>
            <tr>
              <td><%= book_rank.order_id %></td>
              <td><%= book_rank.book.name %></td>
              <td><%= book_rank.view %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
    <%= paginate @ranks %>
  </body>
</html>
